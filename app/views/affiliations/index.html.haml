%h1 所属一覧

%table
  %thead
    %tr
      %th
        ID
      %th
        所属名
  %tbody
    - @affiliations.each do |f|
      %tr
        %td
          = f.id
        %td
          = f.name